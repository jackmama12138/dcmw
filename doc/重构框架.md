# 以192.168.110.111服务器为网关ip地址为例

# 目录结构:
    dmcm:
        client:客户端
        gateway:网关
        worker:执行器        

# 客户端配置信息:
    SERVER_PUBLISH_URL=192.168.110.1/task/add

# 客户端/任务发布: 只和网关通信
    POST 192.168.110.111/task/add
    {
        task_id: 1,
        target_url: "https://live.douyin.com/581239137637",
        task_type: "AFK",
        pipeline:[
            { type: 'navigate', url: 'https://live.douyin.com/',waitUntil: 'commit' },
            { type: 'wait', min: 8000 ,max: 15000},
            { type: 'navigate', url: 'https://live.douyin.com/581239137637',waitUntil: 'commit' }
        ],
        count: 50,
        task_time:3600,任务持续时间 单位秒
    }
# 客户端技术栈:
    vue3 + axios + tailwindcss

# WORKER端配置信息:
    # 谷歌浏览器官方稳定版的 Mac 绝对路径
    MAC_CHROME_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
    # 账号 Profile 主基础绝对路径
    CHROME_PROFILES_BASE_DIR="/Users/showmaker/ChromeProfiles"
    # 账号 Profile 名称
    CHROME_PROFILE_NAME=Profile1,Profile2,Profile3,Profile4,Profile5,Profile6,Profile7
    # 工作机 以IP为ID
    WORKER_ID=192.168.110.1
    # WSS 服务器地址
    SERVER_HOST=ws://192.168.110.111:7777/ws/livetop
    # 网关通知APi地址
    CENTER_NOTIFY_URL=http://192.168.110.111:7777/notify
    函数中的随机默认等待时间min,max
    WAIT_TIME=3000,4000
    日志存储路径
    LOG_DIR=logs
    -- wss连接到网关保持通信
    -- 任务结束通知http://192.168.110.111:7777/notify
    -- 心跳和任务状态信息通过wss给网关 node执行状态和执行target_url执行到哪一步不要了只要执行结果

# worker端技术栈:
    nodejs + socket.io + playwright + winston + dotenv
    dotenv: 配置文件需要◇ injected env (3) from .env // tip: ◈ secrets for agents [www.dotenvx.com]错误信息忽略
    logger: winston 清理超过7天的日志 按天存储 格式：worker-2026-05-27.log
    socket.io: 
        ws://192.168.110.111:7777/ws/livetop?worker_id=192.168.110.1告诉网关你是谁
        http://192.168.110.111:7777/notify任务结束通知格式如下{node_name:Profile1,status:success,target_url:https://live.douyin.com/581239137637}
    playwright: 
        使用launchPersistentContext MAC_CHROME_PATH用指定浏览器打开 userDataDir: CHROME_PROFILES_BASE_DIR + CHROME_PROFILE_NAME
        任务函数:根据函数功能要处理边界情况 防御性编程
                运行时防崩溃与异常平滑降级
                可以参考playwright-cli的封装
            open(url):
                -- 打开浏览器 如果页面已经打开则使用第一个页面，否则创建一个新页面  const page = context.pages()[0] ? context.pages()[0] : await context.newPage();
            wait(min,max)
                -- 加入随机等待时间
            reload()
                -- 重新加载当前页面
            close()
                -- 关闭当前页面和浏览器。
            click(string)
                -- 对原生api的封装 内置防卡死包装 可以参考playwright-cli的封装
                -- "#main > button.submit也支持元素选择器
                -- 支持这样的写法 { type: 'click', "string": "getByText('测试')" }
                getByAltText
                getByLabel
                getByPlaceholder
                getByRole
                getByTestld
                getByText
                getByTitle
            dblclick(string)
                -- 双击元素
            goto(url)	
                -- 导航到 URL
            hover
                -- 悬停在元素上
            eval
                -- 执行js代码
            run-code
                -- playwright 代码
            mousemove <x> <y>	将鼠标移动到像素坐标
            mousedown [button]	按下鼠标按钮（左、中、右）
            --更多我可能用到的

# 网关配置信息:
    REDIS_HOST=192.168.110.111
    REDIS_PORT=6379
    REDIS_PASSWORD=
    PORT=7777

# 网关技术栈:
    nodejs + socket.io  + winston + dotenv + express + redis + cros
    dotenv: 配置文件需要◇ injected env (3) from .env // tip: ◈ secrets for agents [www.dotenvx.com]错误信息忽略
    logger: winston 清理超过7天的日志 按天存储 格式：worker-2026-05-27.log
    redis: 存储任务信息 
    express: 定义路由和api接口 

# 资源管理准则 (Life-cycle Principle)：

    所有浏览器实例必须具备优雅关闭接口。

    //todo 新增规则 严禁出现 死循环

    必须处理好进程生命周期，严禁内存泄漏或产生“孤儿进程”。
# 分布式鲁棒性 (Distributed Robustness)：

    Worker 端必须实现心跳上报（Heartbeat），以便控制端监控状态。

    任务必须支持原子化处理，且在 Redis 中保持状态，支持失败后的断点续传。

# 交互风格约束：

    在生成代码时，优先提供“Pipeline 编排视角”下的解决方案。

    对复杂业务逻辑，请给出“插件实现”和“任务编排配置”两部分代码。

    若面临风控挑战（如检测继续播放、悬浮触发元素），请强制建议注入安全 Hook 策略，并解释其背后的风控对抗原理。

# workrer:运行前置检查:
    检查配置信息中的账号 Profile 配置文件是否存在
    检查配置信息MAC_CHROME_PATH中的浏览器是否存在

# gateway运行前置检查:
    redis能否正常连接
    
            
            